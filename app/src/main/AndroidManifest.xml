<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.udicity.shams.tourinsuez">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".NavigationDrawerPackage.ScrollingActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".NavigationDrawerPackage.NavigationDrawerActivity"
            android:label="@string/title_activity_navigation_drawer"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".AboutSuez.AboutSuezActivity" />
        <activity android:name=".Help.HelpActivity" />
        <activity android:name=".hotel.HotelActivity" />
        <activity android:name=".Restaurant.RestaurantActivity" />
        <activity android:name=".Hospital.HospitalActivity" />
        <activity android:name=".Shopping.ShoppingActivity" />
        <activity android:name=".TourismPlaces.TourismActivity" />
        <activity android:name=".Events.EventsActivity"></activity>
    </application>

</manifest>